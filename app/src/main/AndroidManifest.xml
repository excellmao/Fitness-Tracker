<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitnessTracker"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBJ5uTj9fJoedzlnZ9zR1Jp8zBpCHK91EU" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.fitnesstracker.workout.ActiveWorkoutActivity"
            android:exported="false"
            android:theme="@style/Theme.FitnessTracker" />

        <activity
            android:name="com.example.fitnesstracker.nutrition.LogFoodActivity"
            android:exported="false" />

        <activity
            android:name="com.example.fitnesstracker.run.ActiveRunActivity"
            android:exported="false" />

        <activity
            android:name="com.example.fitnesstracker.run.RunSummaryActivity"
            android:exported="false"
            android:theme="@style/Theme.FitnessTracker" />

        <receiver
            android:name=".nutrition.WaterLogReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="ADD_WATER" />
            </intent-filter>
        </receiver>

    </application>

</manifest>